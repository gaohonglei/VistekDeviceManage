import xml.etree.cElementTree as ET
import sys
device=ET.Element("devices")
subdevice=ET.SubElement(device,"device")
subdevice.set("name","ghl")
subdevice.set("age","26")
subdevice.text="false"

subdevice=ET.SubElement(device,"device")
subdevice.set("name","cy")
subdevice.set("age","27")
subdevice.text="true"

status_xml = ET.tostring(device, encoding='UTF-8', method='xml')

v=[1,2,3]
b=v
c=[]

c.append(v)
print(sys.getrefcount(v))
del c

print(sys.getrefcount(v))
